#include "main.h"

/**
 * positive_or_negative - test if the correct output when given a case of 0
 * Return: 0
 */

positive_or_negative(void)
{
	int i;

	i = 98;
	positive_or_negative(i);

	return (0);
}
