#include <stdio.h>

/**
 * main - prints the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	char ll;

	for (ll = 'a' ll <= 'z'; ll++)
		putchar(ll);

	putchar('\n');

	return (0);
}
