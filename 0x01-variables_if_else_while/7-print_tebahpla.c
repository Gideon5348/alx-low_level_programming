#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse, followed by a new line.
 *
 * Return: Always (0)
 */
int main(void)
{
	char ll;

	for (ll = 'z'; ll => 'a'; ll--)
		putchar(ll);

	putchar('\n');

	return (0);
}
